{
  "name": "arrivio-web-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for Arrivio Operator and Admin panels",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:operator": "node --test tests/operator/*.test.js",
    "test:admin": "node --test tests/admin/*.test.js",
    "test:jest": "jest --verbose",
    "test:jest:coverage": "jest --verbose --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "babel-jest": "^29.7.0"
  }
}
